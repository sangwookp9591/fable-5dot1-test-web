# 07 · Browser Test — 실제 브라우저에서 확인한 것

도구: Chrome DevTools MCP (스크린샷 · 콘솔 · 성능 트레이스 · Lighthouse), 개발 서버 :3210, 프로덕션 서버 :3211.

## 1차 (원시 도형 방 + 첫 배치)

| 본 것 | 문제 | 고친 것 |
|---|---|---|
| 인트로 1440 | 제목이 모니터·TV 위에 겹쳐 안 읽힘. 의자가 화면 중앙을 가림. 벽 위로 천장 없이 배경색이 보임 | 인트로 카피에 panel, 카메라를 뒤·위로, 천장 추가 |
| 경력 1440 | 3D 가 사라짐 → HMR 로 캔버스가 바뀔 때 옛 컨텍스트의 `webglcontextlost` 가 `failScene()` 을 호출 | ContextGuard 로 옮기고 `isConnected` 확인, cleanup 추가 |
| 경력 1440 | 벽 보드의 막대가 라벨과 z-fight 로 검게 깨짐 | 막대 z +0.004, 라벨 뒤에서 시작 |
| 경력 1440 | 아잉이 flip 되어 콘텐츠 반대쪽을 가리킴 | 콘텐츠가 왼쪽인 섹션은 flip 없음 (studio 만 flip) |
| ZIVO 1440 | 패널이 뷰포트보다 길어 슬라이더가 잘림. 카운트가 439 에서 멈춤 | 2단계 레이아웃(숫자 → 슬라이더), 끝값 스냅 |
| 고치는 과정 1440 | 모니터가 패널 뒤에 완전히 숨어 3D 가 의미 없음 | 아잉을 3D 책상 자리에 앵커 + 책상 윗면 가림, 모니터가 단계를 미러링 |
| 스튜디오 1440 | 마지막 정거장(노트북)에 도달하기 전에 다음 섹션으로 블렌드 시작 | 투어 0~0.8, 0.86 부터 블렌드 |
| 스튜디오 1440 | TV 타일이 TV 아래로 흘러나옴. 라운지 체어가 카메라 앞을 막음 | 타일 y 오프셋, 라운지 체어를 앞쪽 왼쪽으로 |
| AI 1440 | 포스터가 갈색 판으로만 보임 → 프레임 박스가 포스터 앞에 있었음 | 프레임 z -0.015 |
| 검수 1440 | 보드가 길어 측정값이 잘림 | 2열 그리드 |
| 결과 1440 | 아잉이 중앙 보드를 가림. 보드 셀이 이중 테두리 | 아잉 오른쪽, `.result-board > div` |
| 모바일 390 인트로 | 3포인트 2열 그리드가 좁아 줄바꿈이 어색 | 640px 아래 1열 |

## 사용자 피드백 반영

- "걸어나올 때 프레임이 끊기는 느낌" → `canplay` → `canplaythrough` 로 재생 시작 조건 강화, left/bottom → transform 이동, 캐시 예열
- "배경 오브젝트가 현실과 너무 다름" → 원시 도형 → Kenney 가구 GLB + 그림자 + 나무 바닥 텍스처

## 2차 (프로덕션 빌드 :3211, 자동화 검증)

| 검증 | 결과 |
|---|---|
| 인트로 타임라인 (initScript 로 0.6s 간격 샘플) | 포스터 0~0.6s → enter 0.6~5.4s → idle. 대사 1 은 2.4~5.0s, 대사 2 는 5.2~8.4s |
| 처음엔 대사가 안 나왔다 | 인트로 effect 가 ref 로 "한 번만" 을 판단해 StrictMode 이중 실행에서 타이머가 지워짐 → enter 종료/섹션 이탈 시점에 플래그를 세우는 idempotent 구조로 변경 |
| 섹션 점프 후 아잉 버퍼 (career → intro → loop → zivo) | point → idle → type → idle. 활성 video 1개만 opacity 1, 나머지 src 비움 |
| 1.5초 역방향 전체 스크롤 후 3초 | 섹션 = 시작, 아잉 = idle |
| `?no3d=1` `?novideo=1` `?reduced=1` | 정적 배경 / WebP / 정지 포스터. 콘솔 0건 |
| 콘솔 (프로덕션 전체 스크롤) | error 0 · warning 0 |
| Lighthouse desktop | A11y 96(대비 수정 후 재측정) · Best Practices 100 · SEO 100 |
| 전체 스크롤 누적 CLS | 0.237 → stage 상단 고정 + 아잉 transform scale 로 0.015 (06-performance 참조) |
| Lighthouse desktop 재측정 | A11y 100 · Best Practices 100 · SEO 100 |

## 3차 (사용자 캡처 피드백 반영)

| 본 것 | 원인 | 고친 것 |
|---|---|---|
| 스크롤 중 말풍선이 잘림 | 아잉 컨테이너의 `contain: layout paint` 가 컨테이너(400px 프레임) 밖으로 나가는 말풍선을 그리지 않음. 컨테이너를 `scale()` 로 줄일수록 역스케일된 말풍선이 더 크게 벗어남. 가장자리(x=8vw, 86vw)에서는 뷰포트 밖으로도 나감 | 말풍선을 스케일·반전되는 컨테이너 밖 형제 요소로 빼고 뷰포트 px 로 배치. 폭을 재서 화면 안으로 밀고(`dx`), 꼬리는 `--tail-x` 로 캐릭터 머리 위를 계속 가리킴 |
| 고치는 과정에서 아잉이 머리만 보임 (사용자 캡처) | 책상 뒤 앵커의 앉은 높이 0.5m → 책상 윗면 0.78 아래가 전부 가려져 상반신이 사라짐 | 앉은 높이 0.68 (발·무릎만 가림, 노트북과 상반신 보임). 가림선 위로 45% 미만이면 앵커를 포기하고 DOM 위치로 |
| 1024×768 에서 패널이 아래로 잘림 | 세로 768 에 11vh 상단 여백 + 큰 타이포 | `(min-width: 901px) and (max-height: 820px)` 축약 블록 |
| 375×667 에서 대부분 섹션이 아래로 잘림 (자동 스윕: career +401, zivo +238, ai +241, review +123, result +77) | 모바일 stage 여백 56+120, 설명 문단·펼침 텍스트 | Opus 5 에이전트에 CSS 축약 위임 → 스윕으로 검증 후 Fable 5.1 이 검토 |
